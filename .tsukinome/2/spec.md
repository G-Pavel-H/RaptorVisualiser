# Spec — Issue #2: Add deleteBuild method to API service

**Type:** feature

> Generated by Tsukinome. This file is the source of truth for the work; edits here drive the build.

## Summary

Add a deleteBuild(buildId) method to the frontend ApiService that issues an HTTP DELETE request to the /api/builds/:id endpoint and returns the HttpClient observable emitting a success acknowledgement, matching the existing CRUD method patterns.

## Requirements

Each requirement is tagged with a confidence level (explicit / inferred / assumption / unknown).

- **R1** `[explicit]` ApiService in frontend/src/app/services/api.service.ts must expose a public deleteBuild(buildId: string) method.
- **R2** `[explicit]` deleteBuild must issue an HTTP DELETE request to `${API_BASE}/api/builds/${buildId}`.
- **R3** `[explicit]` deleteBuild must return the HttpClient observable directly, consistent with getBuild/query methods.
- **R4** `[inferred]` The method must not subscribe to or otherwise consume the observable itself, leaving that to callers as with existing methods.
- **R5** `[explicit]` The observable should emit a success acknowledgement response for the DELETE operation.

## Acceptance Criteria

### AC1

- **Given** An ApiService instance and a valid build id
- **When** deleteBuild(id) is called
- **Then** An HTTP DELETE request is sent to `${API_BASE}/api/builds/${id}`

### AC2

- **Given** deleteBuild(id) is called
- **When** the method returns
- **Then** It returns the HttpClient observable (not a subscription or resolved value), consistent with getBuild/query

### AC3

- **Given** A build id containing characters requiring URL handling
- **When** deleteBuild(id) is called
- **Then** The id is placed into the URL path following the same interpolation approach as the other build methods

### AC4

- **Given** The backend DELETE responds successfully
- **When** a caller subscribes to the returned observable
- **Then** The observable emits a success acknowledgement response

## Non-Goals

- No UI or button changes.
- No backend endpoint work.
- No new error-handling, confirmation, or retry logic beyond what existing service methods provide.

## Edge Cases

- Empty or whitespace-only build id passed to the method.
- Build id containing special characters that may need URL encoding.
- Consistency of behavior if the DELETE returns a non-2xx response (handled by callers, not this method).

## Assumptions

- URL encoding of the build id mirrors the existing methods' approach rather than adding new encoding behavior.

## Open Questions

_None._
