# FastAPI stack
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1
sse-starlette==2.1.3

# Data layer (used from phase 4 onward; pinned now for one-shot installs)
motor==3.6.0

# RAPTOR + its transitive needs.
# The library is installed from GitHub. Its own requirements.txt pins old
# numpy/transformers/openai. We deliberately do NOT mirror those pins here
# beyond what RAPTOR itself drags in — RAPTOR's pip install will do that.
raptor @ git+https://github.com/parthsarthi03/raptor.git@main

# Test
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
